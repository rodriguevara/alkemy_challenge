<div id="<%= dom_id genre %>">
  <p>
    <strong>Name:</strong>
    <%= genre.name %>
  </p>

  <p>
    <strong>Movies:</strong>
    <% genre.movies.each do |movie| %>
      <%= movie.name %> |
      <br>
    <% end %>
  </p>

</div>
